CREATE TABLE EMPLOYEE
        (
        id NUMBER(10) NOT NULL,
        name VARCHAR2(50) NOT NULL,
        PRIMARY KEY(id)
        );

Create Table Product(
    id number(10) Not Null,
    name varchar2(50) not null,
    price decimal(10,4),
    Primary Key(id)
);
